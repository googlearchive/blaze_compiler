functions:
  - isLoggedIn( ): auth.id !== null

schema:
  type: object
  constraint: "false"
  properties:
    childString: {type: string}
    childObject:
      type: object
      additionalProperties: false
      properties:
        grandchildNumber: {type: number}

  $wildchild: {type: string}
  required: [child_object]

access:
  - location: "/"
    read:  "true" #todo, allow boolean in read and write rules
    write: isLoggedIn()
