specification:
  - initial:
      users:
         fred: {item: gold}
         bill: {item: null}
         greg: {item: null}
    substutions: 
      $user: [fred, bill, greg]
    specs:
      - > 
        !EF (root.users.fred.item.val() != null && 
        users.fred.val() = null)
