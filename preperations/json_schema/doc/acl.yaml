predicates:
  isUser(id): auth.id === id 
schema:
  ...
access:
  - location: /users/$user/*
    read:  isUser($user)  
    write: isUser($user)  
    
  - location: /users/$user/public/*
    read:  isLoggedIn()
   
