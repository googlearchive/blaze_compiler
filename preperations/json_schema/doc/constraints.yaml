predicates:
  inMaintenance(): root.system.maintenance.exists()
  
schema:
  definitions:
    Log:
      type: object
      $entry:
        type: object
        constraint: > 
          !prev.exists() || 
          prev.val() === next.val() || 
          inMaintenance()
   
