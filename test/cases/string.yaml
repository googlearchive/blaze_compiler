schema:
  type: string
  enum: ["correct", "unknown"]
  examples:
    - "correct"
  nonexamples:
    - true
    - notcorrect
    - {a: "string"}


access:
  - location: "/"
    read:  "true"
    write: "true"
