schema:
  type: object
  $chld:
    type: string
    constraint: next.val().contains('Y') #todo internal quotes can't be ""

  properties:
    extra: {type: string}


access:
  - location: "/*"
    read:  "true"
    write: "true"
